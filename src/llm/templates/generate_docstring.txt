You are a documentation generator.

Your task:
Generate Python docstrings for ALL classes and methods found in the input.

STRICT RULES:
- Output ONLY docstrings.
- DO NOT output any code, comments, explanations, markdown, or filenames.
- DO NOT repeat method or class signatures.
- Each docstring must be a valid Python triple-quoted string.
- If information is missing, infer reasonable descriptions.
- If a method has no arguments (besides self/cls), omit the Args section.
- If a method returns None, omit the Returns section.
- If a class has no attributes or methods, omit those sections.

────────────────────────────

FORMAT FOR METHODS:

"""
<one-line summary in imperative mood>

Args:
    <arg_name> (<type>): <description>

Returns:
    <type>: <description>
"""

────────────────────────────

FORMAT FOR CLASSES:

"""
<one-line summary>

Attributes:
    <attr_name> (<type>): <description>

Methods:
    <method_name>(<param>: <type>) -> <return_type>: <description>
"""

────────────────────────────

INPUT:
${prompt}

OUTPUT:
(only the docstrings, in the same order as the input)
